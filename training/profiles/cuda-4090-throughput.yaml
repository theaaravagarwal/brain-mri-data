schema_version: 1
profile_id: cuda-4090-throughput
accelerator: cuda
expected_gpu_name_contains: RTX 4090
mixed_precision: fp16
batch_size: 4
effective_batch_size: 4
num_workers: 8
prefetch_factor: 2
persistent_workers: true
pin_memory: true
patch_size: [80, 80, 80]
notes: Dual-queue profile with eight workers per run; measured 16-worker runs exhaust 32 GiB host RAM and trigger systemd-oomd while the locked effective batch size remains four
